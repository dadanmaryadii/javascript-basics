var name = 'Dimitri';

// && (AND)
// || (OR)
// ==, ===, !=, !==

if ( name[name.length - 1] === ' ' || name[0] === ' ' ) {
  // If TRUE
  console.log('Invalid name');
} else {
  console.log('Valid name');
}