var numbers = [10, 4, 9, 80];
var names = ['foo', 'bar'];
var statuses = [true, false];

// Index and Element (Value)

// Reference
var a = [1, 2, 3];
var b = a;
var c = b;

c.push(90);

console.log(a);

// Built-in functions and properties
// .push() - Add a new element
// .slice() - Slice an array
// .join() - Convert from Array to String
// .pop() - Remove last element
// .splice() - Remove element by index
// .length - Return number of elements in Array
